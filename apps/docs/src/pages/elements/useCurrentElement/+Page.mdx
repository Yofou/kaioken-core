import { UseCurrentElementExample } from '$/examples/useCurrentElement'

# useCurrentElement
grabs the nearest descendent element of what ever the current element is

## Demo

<UseCurrentElementExample />

## Usage
```tsx twoslash
import { useCurrentElement } from '@kaioken-core/hooks'
// ---cut---
const current = useCurrentElement()
```
