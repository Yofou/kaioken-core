import { UseRootNodeExample } from '$/examples/useRootNode'

# useRootNode
Grabs the kaioken Root node and only filters for components in the tree

## Demo
<UseRootNodeExample />

## Usage
```tsx twoslash
import { useRootNode } from '@kaioken-core/hooks'
// ---cut---
const node = useRootNode()
```
