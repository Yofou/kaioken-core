import { UseWindowSizeExample } from '$/examples/useWindowSize'

# useWindowSize
provides the current size of the window on the user monitor

## Demo
<UseWindowSizeExample />

## Usage
```tsx twoslash
import { useWindowSize } from '@kaioken-core/hooks'
// ---cut---
const { width, height } = useWindowSize()
```
