import { UseSaraDemo } from '$/examples/useSaraDemo'
import { UseIntersectionObserverExample } from '$/examples/useIntersectionObserver'

# useIntersectionObserver
Detects that a target element's visibility.


## Demo
<UseIntersectionObserverExample />
<UseSaraDemo />

## Usage
```tsx twoslash
import { useRef } from 'kaioken'
import { useIntersectionObserver } from '@kaioken-core/hooks'
// ---cut---
const ref = useRef<HTMLElement | null>(null)
const controls = useIntersectionObserver(ref, (intersections) => {
  // code here
})
```
